/**
 * Auto-generated by Dark Factory
 * TelegramChatHistory API Routes
 */

import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/db';

export const dynamic = 'force-dynamic';


// GET - List all TelegramChatHistory records
export async function GET(request: NextRequest) {
  try {
    const searchParams = request.nextUrl.searchParams;
    const page = parseInt(searchParams.get('page') || '1');
    const limit = parseInt(searchParams.get('limit') || '20');
    const skip = (page - 1) * limit;

    const [items, total] = await Promise.all([
      prisma.telegramChatHistory.findMany({
        skip,
        take: limit,
        orderBy: { createdAt: 'desc' },
      }),
      prisma.telegramChatHistory.count(),
    ]);

    return NextResponse.json({
      data: items,
      pagination: {
        page,
        limit,
        total,
        totalPages: Math.ceil(total / limit),
      },
    });
  } catch (error) {
    console.error('Error fetching TelegramChatHistory:', error);
    return NextResponse.json(
      { error: 'Failed to fetch TelegramChatHistory records' },
      { status: 500 }
    );
  }
}


// POST - Create a new TelegramChatHistory
export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    
    // Validate required fields
    const required = ['chatId', 'userId', 'message', 'direction', 'context'];
    const missing = required.filter(field => !(field in body));
    if (missing.length > 0) {
      return NextResponse.json(
        { error: `Missing required fields: ${missing.join(', ')}` },
        { status: 400 }
      );
    }

    const telegramChatHistory = await prisma.telegramChatHistory.create({
      data: body,
    });

    return NextResponse.json(telegramChatHistory, { status: 201 });
  } catch (error) {
    console.error('Error creating TelegramChatHistory:', error);
    return NextResponse.json(
      { error: 'Failed to create TelegramChatHistory' },
      { status: 500 }
    );
  }
}
